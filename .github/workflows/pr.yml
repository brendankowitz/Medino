name: PR Validation

on:
  pull_request:
    branches: [ master, main ]
    paths:
      - 'src/**'
      - '.github/workflows/**'

jobs:
  validate:
    name: Build and Test PR
    uses: ./.github/workflows/build-and-test.yml